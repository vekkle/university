Типы данных в python:
int	
float	
str	
bool	
list	+
dict	+
set	?
tuple	-
Если данные изменяемые, то тогда их передача в функцию происходит через ссылку (то есть изменяется тот объект, что мы вызвали).
Если данные неизменяемые, то тогда их передача в функцию происходит через значение (то есть создаётся копия сущности в функции, и изменяется эта копия, а исходная сущность не изменяется).

list(range(10))
range(10) #занимает меньше места в памяти

for index, value in enumerate(list(range(10)):
	print(index, value)
	
for key, value in d.items():
	print(key, value)

a, *b, c = [1, 2, 3, 4, 5]
a = 1
b = [2, 3, 4]
c = 5

a, b, c = [1, 2, 3, 4, 5]
error

Исключение — это тип данных.

l = [...] (n элементов списка)
i = ...
if i in l:
какова сложность такого кода?
сложность меньше или равна n
while i < len(l):
	if elem == l[i]: 
		print(i)
		break
	i += 1
O(1) #операция сравнения двух чисел константна, она выполняется за единицу времени
C * n = общая сложность алгоритма
O(n) — максимальная сложность алгоритма (то есть в худшем случае нам нужно пробежать по списку n раз)

x = False == False in [False]
x = True
#потому что False == False будет равняться True, False in [False] будет равняться True, в целом True * True = True

a = 3
1 < a < 5
True

#Для Unix-команд в iPython можно использовать ! перед командой. Пример для Ubuntu:
!gedit  file.txt

f = open('file.txt', 'r', encoding='utf-8')
f.close()

with open('file.txt') as file:
	for line in file:
		print(line, end='') #заменяет последний символ (тут /n) на другой, тогда будет печатать без дополнительных пустых строк
#Лучше открывать с try, потому что функция генерирует исключения.

